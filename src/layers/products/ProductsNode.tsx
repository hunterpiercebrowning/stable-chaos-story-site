import { GenericNode } from '../GenericLayer';
import './products.css';

/** Placeholder: the base node card. WS5 — Products replaces the body of this file. */
export const ProductsNode = GenericNode;
