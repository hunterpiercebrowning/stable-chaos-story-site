import { GenericNode } from '../GenericLayer';
import './services.css';

/** Placeholder: the base node card. WS4 — Services replaces the body of this file. */
export const ServicesNode = GenericNode;
