import { GenericLayer } from '../GenericLayer';
import './services.css';

/**
 * Placeholder: re-exports the generic grid so the layer is navigable today.
 * WS4 — Services replaces the body of this file without touching the registry.
 */
export const ServicesLayer = GenericLayer;
