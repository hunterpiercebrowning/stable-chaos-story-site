import { GenericNode } from '../GenericLayer';
import './background.css';

/** Placeholder: the base node card. WS7 — Foundational Background replaces the body of this file. */
export const BackgroundNode = GenericNode;
