import { GenericLayer } from '../GenericLayer';
import './background.css';

/**
 * Placeholder: re-exports the generic grid so the layer is navigable today.
 * WS7 — Foundational Background replaces the body of this file without touching the registry.
 */
export const BackgroundLayer = GenericLayer;
