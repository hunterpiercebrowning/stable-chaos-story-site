import { GenericNode } from '../GenericLayer';
import './operations.css';

/** Our Operational Advantage has no nodes; the generic card keeps the registry complete. */
export const OperationsNode = GenericNode;
