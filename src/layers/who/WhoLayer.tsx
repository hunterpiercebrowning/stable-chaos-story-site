import { GenericLayer } from '../GenericLayer';
import './who.css';

/**
 * Placeholder: re-exports the generic grid so the layer is navigable today.
 * WS1 — Who We Are replaces the body of this file without touching the registry.
 */
export const WhoLayer = GenericLayer;
