import { GenericLayer } from '../GenericLayer';
import './sectors.css';

/**
 * Placeholder: re-exports the generic grid so the layer is navigable today.
 * WS3 — Critical Sectors replaces the body of this file without touching the registry.
 */
export const SectorsLayer = GenericLayer;
