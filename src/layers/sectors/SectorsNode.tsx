import { GenericNode } from '../GenericLayer';
import './sectors.css';

/** Placeholder: the base node card. WS3 — Critical Sectors replaces the body of this file. */
export const SectorsNode = GenericNode;
