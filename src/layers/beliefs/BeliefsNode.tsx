import { GenericNode } from '../GenericLayer';
import './beliefs.css';

/** Placeholder: the base node card. WS2 — What We Believe replaces the body of this file. */
export const BeliefsNode = GenericNode;
