import { GenericLayer } from '../GenericLayer';
import './beliefs.css';

/**
 * Placeholder: re-exports the generic grid so the layer is navigable today.
 * WS2 — What We Believe replaces the body of this file without touching the registry.
 */
export const BeliefsLayer = GenericLayer;
